const api='/.netlify/functions/staff-session';
async function session(){const r=await fetch(api,{cache:'no-store',credentials:'same-origin'});return r.ok&&(await r.json()).authenticated;}
const areas=['mesero.html','cocina.html','admin.html'];
function area(){return document.querySelector('input[name=area]:checked')?.value||'mesero.html';}
async function logout(){try{await window.stopStaffPush?.();await fetch(api,{method:'DELETE'});}finally{location.href='staff-login.html';}}
async function loginPage(){document.getElementById('login-form').addEventListener('submit',async e=>{e.preventDefault();const button=e.submitter,message=document.getElementById('login-message');button.disabled=true;message.textContent='Comprobando NIP…';try{const r=await fetch(api,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({pin:document.getElementById('staff-pin').value})});const data=await r.json();if(!r.ok)throw Error(data.error||'No se pudo iniciar sesión.');location.href=areas.includes(area())?area():'mesero.html';}catch(error){message.textContent=error.message;document.getElementById('staff-pin').value='';}finally{button.disabled=false;}});}
async function protect(){try{if(!await session()){location.replace('staff-login.html');return false;}document.getElementById('staff-lock')?.remove();const bar=document.createElement('div');bar.className='fixed bottom-2 left-2 z-[100] bg-white text-purple-900 rounded-xl px-3 py-2 text-xs font-bold shadow';bar.innerHTML='🔐 Personal · <button id="enable-push" class="underline">Activar avisos</button> · <button id="quick-logout" class="underline">Salir</button><p id="push-status" class="max-w-xs font-normal"></p>';document.body.appendChild(bar);document.getElementById('quick-logout').onclick=logout;document.getElementById('enable-push').onclick=()=>window.enableStaffPush?.();return true;}catch{location.replace('staff-login.html');return false;}}
if(document.body.dataset.staffLogin==='true')loginPage();
if(document.body.dataset.protected==='true')window.staffReady=protect();
